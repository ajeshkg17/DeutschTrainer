<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/deutschtrainer_icon"
        android:roundIcon="@drawable/deutschtrainer_icon"
        android:label="Deutsch Trainer"
        android:theme="@style/Theme.DeutschTrainer">

        <activity
            android:name=".TrainerActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <activity
            android:name=".BackupRestoreActivity"
            android:exported="false"
            android:parentActivityName=".TrainerActivity" />

        <activity
            android:name=".AiErrorDialogActivity"
            android:exported="false"
            android:theme="@style/Theme.DeutschTrainer.Dialog" />

        <activity
            android:name=".MainActivity"
            android:exported="false" />
    </application>
</manifest>
