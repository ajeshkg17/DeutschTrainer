name: Build DeutschTrainer V4
on:
  workflow_dispatch:
  push:
    branches: [ "main", "html-first-v4", "drive-sync-v4", "voice-inline-v4" ]
    paths:
      - "appv4/**"
      - "scripts/**"
      - "prototypes/**"
      - "settings.gradle.kts"
      - ".github/workflows/build-v4.yml"

jobs:
  build-v4:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Validate HTML app JavaScript
        run: |
          node --check appv4/src/main/assets/core.js
          node --check appv4/src/main/assets/quiz.js
          node --check appv4/src/main/assets/learn.js
          node --check appv4/src/main/assets/settings.js
          node --check appv4/src/main/assets/drive-integration.js
          node --check appv4/src/main/assets/drive-sync.js
          node --check appv4/src/main/assets/boot.js
          node --check appv4/src/main/assets/voice-fix.js
          grep -q "V4 · HTML-FIRST BUILD" appv4/src/main/assets/core.js
          grep -q "Google Drive sync" appv4/src/main/assets/drive-integration.js
          grep -q "No silent overwrites" appv4/src/main/assets/drive-sync.js
          grep -q "Nothing is erased" appv4/src/main/assets/voice-fix.js
      - name: Inject Drive bridge into V4 MainActivity
        run: python3 scripts/inject-drive-sync.py
      - name: Inject inline voice recognition into V4 MainActivity
        run: python3 scripts/inject-inline-speech.py
      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Set up Gradle 8.9
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Restore stable V4 debug signing key
        uses: actions/cache@v4
        with:
          path: ~/.android/debug.keystore
          key: deutschtrainer-v4-debug-signing-v1
      - name: Create signing key if needed
        run: |
          mkdir -p ~/.android
          if [ ! -f ~/.android/debug.keystore ]; then
            keytool -genkeypair -v -keystore ~/.android/debug.keystore -storepass android -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=Android Debug,O=Android,C=US"
          fi
      - name: Print V4 OAuth signing fingerprint
        run: keytool -list -v -keystore ~/.android/debug.keystore -storepass android -alias androiddebugkey | grep -E "SHA1:|SHA-1:"
      - name: Build only the fresh V4 module
        run: gradle :appv4:assembleDebug --stacktrace
      - name: Upload V4 APK
        uses: actions/upload-artifact@v4
        with:
          name: DeutschTrainer-V4-VoiceFix
          path: appv4/build/outputs/apk/debug/appv4-debug.apk
